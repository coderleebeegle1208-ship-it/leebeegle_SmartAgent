{
  "name": "agent-remote",
  "version": "0.1.0",
  "private": true,
  "description": "Phone-first dashboard to run and monitor Claude Code / Codex on this PC",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node --no-warnings=ExperimentalWarning server/index.js",
    "dev": "node --no-warnings=ExperimentalWarning --watch server/index.js",
    "test": "node --test test/core.test.js"
  },
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {
    "express": "^5.2.1",
    "web-push": "^3.6.7",
    "ws": "^8.21.3"
  }
}
